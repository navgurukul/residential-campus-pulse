# Copy this to .env and fill in your values

# MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/campus-pulse?retryWrites=true&w=majority

# Port (optional, defaults to 3001)
PORT=3001
